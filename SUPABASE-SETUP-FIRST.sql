-- Growthika quick database setup
-- Run the complete supabase-schema.sql file, not only the admin UPDATE query.
-- This helper intentionally contains no secrets.
select 'Open and run supabase-schema.sql completely' as next_step;
